<?php

return [
    /*
     * The locale used when a translation for the requested locale is missing.
     */
    'fallback_locale' => 'ru',

    /*
     * If a translation is missing for both the requested locale and the
     * fallback locale, return any available translation.
     */
    'fallback_any' => true,

    'use_fallback_locale' => true,
];
